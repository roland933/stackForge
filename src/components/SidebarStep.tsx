import type { LucideIcon } from "lucide-react";

type SidebarStepProps = {
    title: string;
    description: string;
    icon: LucideIcon;
    active?: boolean;
};

export function SidebarStep({title,description,icon,active}:SidebarStepProps) {
    
}